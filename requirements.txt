# Minimal requirements for Streamlit UI only (keeps the build fast on Streamlit Cloud).
# Heavy ML / backend dependencies have been moved to backend-requirements.txt

streamlit
pandas
numpy
plotly
altair
python-dotenv
joblib

# If the Streamlit app still fails because it imports other heavy packages at module import
# time, we'll need to lazy-load those imports inside app/streamlit_app.py. I can make
# that change next if you want.
